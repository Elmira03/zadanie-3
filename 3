#include <iostream>
#include <math.h>
using namespace std;
void f(int n, int i)
{
	if (n<=1) {
		return;
	}
	if (n % i == 0)
	{
		cout << i << " ";
		f(n / i, i);
	}
	else {
		++i;
		f(n, i);
	}
}
int main()
{
	int n;
	cin >> n;
	f(n, 2);
}
